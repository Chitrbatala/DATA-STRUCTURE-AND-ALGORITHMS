class Solution {
    public void printTillN(int n) {
        helper(n, n);
    }
    private void helper(int curr, int n) {
        if (curr < 1) {
            return;
        }
        helper(curr - 1, n);
        System.out.print(curr + " ");
    }
}

            // Recursion
        // private void helper(int curr, int n) {
        // if (curr > n) {
        //     return;
        // }
        // System.out.print(curr + " ");
        // helper(curr + 1, n);

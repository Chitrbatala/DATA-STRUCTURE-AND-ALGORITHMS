class Solution {
    public Node deleteHead(Node head) {
        // code here
        if (head == null) {
            return null;
        }
        
        Node temp = head;
        
        head = head.next;
        temp = null;
        
        return head;
    }
}
